# Test ook 3
Test how to offset counters using of `ook_modem.h`.  Enforces that `OOK_DELTA_FRAMES` is correct

# Flow
* cs21 outputs counter
* cs20 picks it up
  * testbench knows they were in sync to start with, so calculate offset

