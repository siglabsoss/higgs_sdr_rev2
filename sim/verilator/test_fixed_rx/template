Schedule sch1 [NSLICES] = {  //SOURCE, DESTINATION
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128},
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128},
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128},
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128},
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 0}, 
					(Schedule) {SRC_ADDR, 0x1, ({{perm0_0}} << 12) | (DST_ADDR), 128}
					};   //vector column 15


Schedule sch3 [NSLICES] = {  //SOURCE, DESTINATION
					(Schedule) { ({{perm1_0}} << 12)   | SRC_ADDR, 0x1,   (DST_ADDR), 128},  
					(Schedule) { ({{perm1_1}} << 12)   | SRC_ADDR, 0x1,   (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_2}} << 12)   | SRC_ADDR, 0x1,   (DST_ADDR), 128}, 
					(Schedule) { ({{perm1_3}} << 12)   | SRC_ADDR, 0x1,   (DST_ADDR), 128}, 
					(Schedule) { ({{perm1_4}} << 12)   | SRC_ADDR, 0x1,   (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_5}} << 12)   | SRC_ADDR, 0x1,   (DST_ADDR), 128},  
					(Schedule) { ({{perm1_6}} << 12)   | SRC_ADDR, 0x1,   (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_7}} << 12)   | SRC_ADDR, 0x1,   (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_8}} << 12)  | GARBAGE_ADDR, 0, (DST_ADDR), 128}, 
					(Schedule) { ({{perm1_9}} << 12)  | GARBAGE_ADDR, 0, (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_a}} << 12)  | GARBAGE_ADDR, 0, (DST_ADDR), 128},  
					(Schedule) { ({{perm1_b}} << 12)  | GARBAGE_ADDR, 0, (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_c}} << 12)  | GARBAGE_ADDR, 0, (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_d}} << 12)  | GARBAGE_ADDR, 0, (DST_ADDR4), 128}, 
					(Schedule) { ({{perm1_e}} << 12)  | GARBAGE_ADDR, 0, (GARBAGE_ADDR), 0},  
					(Schedule) { ({{perm1_f}} << 12)  | GARBAGE_ADDR, 0, (DST_ADDR), 128}
					};   //vector column 15

